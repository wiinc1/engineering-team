# 2026-07-24

- Validated GitLab MR !322 from a clean temporary worktree at exact head
  `a9c225b8651f8a044fe32f52257add408fac648e`; preserved the dirty original
  workspace.
- Exact-head focused tests, security (66/66), chaos (20/20), Node/API coverage
  (954/954), UI coverage, source integrity, secret scan, standards,
  maintainability, and coverage policy passed. Suite line coverage was 71.39%.
- Exact-head Vercel frontend preview deployed successfully, but its backend
  health is invalid because the preview environment has a stale database
  endpoint. The repository's canonical factory runtime is the coordinated
  operator stack, not Vercel/Supabase.
- Coordinated operator stack passed exact-SHA release health, Graphile setup,
  LangGraph deep health, and a short dual-runtime soak harness smoke with zero
  violations or connection leaks. The required 24-hour staging soak remains
  outstanding.
- GitLab pipeline #205 remains failed before performance tests because the
  shared runner cannot satisfy its <=25% CPU quiescence gate. Three orphaned
  `partner_portal` Playwright/Next dev-server trees on ports 3105, 3140, and
  3142 consume most host CPU; two have been alive for about 13 hours. Do not
  stop unrelated processes or change shared-runner concurrency without operator
  approval.
- The operator approved graceful termination of those three exact orphan trees;
  they exited cleanly and the host immediately passed five quiescence samples.
- A fresh exact-SHA branch pipeline then exposed a newly published high-severity
  `fast-uri` advisory in the build gate. MR !322 was advanced to
  `939d3de00409d1a1648c3838e9d0f7a4e73c24f0` with commit
  `fix(deps): patch fast-uri security advisory`, updating only the lockfile from
  3.1.2 to 3.1.4. Production audit, SBOM, lint, typecheck, and build passed.
- MR pipeline #340 is queued at the new exact head. The shared runner is again
  saturated because eight legitimate `partner_portal` jobs are running
  `npm ci` concurrently. A temporary runner concurrency reduction requires
  separate operator approval; do not interrupt these active jobs.
- The exact-head Vercel frontend preview is READY at
  `https://engineering-team-adkh50faf-wiinc1-hotmailcoms-projects.vercel.app`;
  root is 200, but backend version health remains invalid because of the known
  stale Vercel database environment. The coordinated stack was rebound to
  `939d3de…` and passed exact-SHA health, UI, Graphile setup, LangGraph deep
  health, and regenerated SBOM checks.
- MR pipeline #340 completed successfully at `939d3de…`. Because the connected
  GitLab API was read-only and protected `main` rejected a direct push, an empty
  bookkeeping commit `5dcda0b048518ed79d715621f6ca69f1c12df28f` was added
  to the source branch with GitLab's native auto-merge push option. Its code tree
  was unchanged.
- MR !322 merged into `main` as merge commit
  `56ed662bfb3df538d402ff7045f17ebae4e5d867`. Gating pipeline #350 and
  post-merge `main` pipeline #352 both passed build, performance evidence,
  Graphile verification, and LangGraph verification. Optional `deploy-review`
  remains manual; production cutover and the required 24-hour staging soak
  remain out of scope and outstanding.
- Removed the temporary verification branch and the three MR !322 validation
  worktrees. Stopped the temporary coordinated stack while preserving shared
  Postgres on port 15432. The dirty original workspace was not modified beyond
  this memory entry.
- Deployed merge `56ed662bfb3df538d402ff7045f17ebae4e5d867` to the
  durable operator-hosted coordinated staging stack from clean worktree
  `/private/tmp/engineering-team-staging-56ed662-uFNvVM`. The API release-health
  endpoint proves the exact commit; Postgres, API, workers, UI, forgeadapter,
  and live OpenClaw are healthy.
- Captured a protected pre-rollout Postgres backup and passed a disposable
  restore plus migrations `022` through `018` down/reapply drill. Live staging
  migrations are applied through `022_langgraph_lifecycle_events.sql`.
- Staging Graphile health acknowledged 375/375 deliveries with zero residual
  rows. LangGraph deep health passed; the focused load completed 985 runs with
  zero failures/duplicates/residual rows. Lifecycle equivalence passed.
- Started the required 24-hour concurrent runtime soak as one-shot launchd
  service `com.engineering-team.runtime-soak-56ed662`, deployment
  `staging-56ed662-20260725T0313Z`, at `2026-07-25T03:11:38Z`; expected
  completion is `2026-07-26T03:11:38Z`. First five-minute window passed:
  Graphile 7,500/7,500 with zero residue and LangGraph 6,762 runs with zero
  failures, duplicates, or cleanup residue. Do not remove the staging worktree
  or unload the soak service while this run is active.
- Stopped that soak with zero credited duration after its second Graphile
  window exited without an artifact while the paired LangGraph window passed.
  An isolated 5-minute Graphile reproduction submitted 7,500 jobs but failed
  `job_runtime_load_completion_timeout` with 1,071 still queued at the drain
  deadline. All scoped test rows were cleaned. The shared host was not
  performance-quiescent (load averages 20.21/17.08/14.72) because unrelated
  active `partner_portal` Playwright/Next jobs were consuming substantial CPU
  and memory. Do not interrupt those jobs without operator approval. Wait for
  the repository's <=25% CPU quiescence gate, rerun the isolated proof, then a
  paired proof, and only then restart the full 24-hour soak from zero.
- The exact quiescence gate then ran for its full 20-minute timeout and never
  achieved five stable samples. Installed and started durable LaunchAgent
  `com.engineering-team.runtime-release-resume-56ed662`, pinned to exact commit
  `56ed662b…`. It waits for the strict host gate, runs captured isolated and
  paired five-minute preflights with scoped residual cleanup, rechecks
  quiescence between stages, and starts a fresh 24-hour soak only after both
  preflights pass. State is in
  `.artifacts/staging-rollout-56ed662/resume-state.json`. The invalid original
  soak launcher was moved into its failed artifact directory so it cannot
  restart at login.
- After three consecutive goal audits, the same external shared-host blocker
  remained: the resume controller had zero consecutive valid CPU samples while
  unrelated GitLab/Next/indexing work held the host well above 25%. Marked the
  release goal blocked per the blocked-audit rule. The launchd controller stays
  active and will resume the exact preflight/soak sequence automatically when
  the host changes state; production remains unapproved.
