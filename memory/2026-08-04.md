# 2026-08-04

- The exact-SHA containerized recovery controller advanced overnight. Isolated
  Graphile attempts 8 and 9 passed 7,500/7,500 with zero residue; attempt 9 had
  enqueue p95/p99 33.869537/55.969985 ms and ready-to-start p95 141 ms. Paired
  attempt 9 passed Graphile (46.062972/87.130989 ms enqueue p95/p99, 141 ms
  ready p95) and LangGraph with zero failures and cleanup residue.
- Two fresh 24-hour soak attempts started but aborted early when a later
  Graphile five-minute window exceeded the harness grace and emitted no
  artifact while the paired LangGraph window passed. The first failed-soak
  cleanup completed. The second stopped at window 2 and left seven Graphile
  jobs with Graphile's four-hour logical crashed-worker locks plus 705 scoped
  deliveries and 599 effects; no worker container or database backend remained.
- Recovered the second failed soak through Graphile Worker's supported public
  `forceUnlockWorkers` and `completeJobs` APIs, then transactionally removed
  exact-tenant application rows. Direct verification found zero Graphile jobs
  and zero `soak_%` deliveries, effects, or operator actions.
- Hardened the external controller so force-unlock requires an explicit
  post-child crash-cleanup authorization, verifies the scoped jobs are unlocked,
  then completes them. Added a tested exact-tenant fallback for failed soak
  cleanup and automatic preservation of expired load-override authorization.
  The crash-cleanup guard test passed and `tests/unit/runtime-soak.test.js`
  passed 5/5.
- Restarted the loaded LaunchAgent in place as PID 89784. It verified the exact
  image/revision and staging database, preserved the expired authorization, and
  is waiting at the two-minute <=25% CPU gate for a new isolated/paired/soak
  cycle. Active GitLab job count remains evidence only and does not block the
  user-authorized loaded-host recovery.
- Added a revision/image/artifact/TTL-bound preflight checkpoint using the
  passing attempt 9 evidence. Before reuse, the controller validates both
  artifact thresholds and zero exact-tenant database residue. Failed soak
  attempts may retry directly while that checkpoint remains no more than six
  hours old; a second freshness check after admission prevents a soak from
  starting if the checkpoint expires while waiting.
- At the user's explicit instruction to continue despite the load, recorded a
  one-use authorization for only the `soak_24h` host-admission gate. The
  LaunchAgent consumed it at 2026-08-04T13:04:04Z and started a fresh exact-SHA
  containerized 24-hour soak (`staging-56ed662-20260804T1304Z`), expected to
  complete at 2026-08-05T13:04:04Z. Test workload and pass budgets were not
  changed. Controller PID was 1640 and soak container was
  `engineering-team-soak-24h-mseo69bo` at startup.
- Soak window 0 passed under the elevated host load. Graphile completed
  7,500/7,500 with enqueue p95/p99 50.455417/136.987816 ms,
  ready-to-start p95 206 ms, and zero cleanup residue. LangGraph ran 300.017
  seconds at concurrency 2, completed 10,561 operations with zero failures or
  duplicate side effects, and passed cleanup. Both window 1 workload children
  were live immediately afterward.
- Soak window 1 also passed: Graphile completed 7,500/7,500 with enqueue
  p95/p99 51.034410/112.092307 ms, ready-to-start p95 318 ms, and zero residue;
  LangGraph ran 300.060 seconds at concurrency 2, completed 7,568 operations
  with zero failures or duplicates, and passed cleanup. Both window 2 workload
  children were live afterward.
- Soak window 2 passed, clearing the exact boundary where the preceding run
  failed without a Graphile artifact. Graphile completed 7,500/7,500 with
  enqueue p95/p99 62.468884/135.508263 ms, ready-to-start p95 579 ms, and zero
  residue. LangGraph ran 300.012 seconds at concurrency 2, completed 9,770
  operations with zero failures or duplicates, and passed cleanup. Window 3
  began with both workload children live.
- Soak window 3 passed. Graphile completed 7,500/7,500 with enqueue p95/p99
  22.238940/86.135126 ms, ready-to-start p95 58 ms, and zero residue.
  LangGraph ran 302.891 seconds at concurrency 2, completed 20,074 operations
  with zero failures or duplicates, and passed cleanup. Window 4 began with
  both workload children live.
- Soak window 4 passed. Graphile completed 7,500/7,500 with enqueue p95/p99
  38.332288/109.874443 ms, ready-to-start p95 162 ms, no ending pool waiters,
  and zero residue. LangGraph ran 300.026 seconds at concurrency 2, completed
  8,183 operations with zero failures or duplicates, no ending pool activity
  or waiters, and passed cleanup. Window 5 began with both children live.
- Soak window 5 passed. Graphile completed 7,500/7,500 with enqueue p95/p99
  45.002390/89.897989 ms, ready-to-start p95 321 ms, no ending pool waiters,
  and zero residue. LangGraph ran 300.010 seconds at concurrency 2, completed
  9,243 operations with zero failures or duplicates, no ending pool activity
  or waiters, and passed cleanup. Window 6 began with both children live.
- The 13:04Z soak failed at window 6 because Graphile did not emit its report;
  LangGraph passed, but Graphile exceeded its completion/drain allowance under
  host load. The controller aborted at 13:42:45Z, force-unlocked and completed
  29 exact-prefix jobs from interrupted window 7, and cleaned 1,602 effects and
  1,902 deliveries. Direct global verification found zero `soak_%` Graphile
  jobs/locks, actions, effects, or deliveries afterward.
- Using the user's standing instruction to continue despite load, recorded and
  consumed another one-use `soak_24h` admission. The still-valid attempt 9
  preflight checkpoint was reused, the failed output was preserved at
  `.artifacts/runtime-soak-preserved-mseplij5`, and a fresh 24-hour retry
  (`staging-56ed662-20260804T1343Z`, container
  `engineering-team-soak-24h-mseplijs`) began at 2026-08-04T13:43:56Z with
  expected completion at 2026-08-05T13:43:56Z. Budgets were unchanged.
- The 13:43Z retry failed in window 0 under extreme shared-host contention.
  LangGraph ran 300.741 seconds with zero operation failures or duplicates, but
  checkpoint-write p95 was 272 ms against the unchanged 250 ms budget and the
  report correctly set `passed: false`; measured throughput collapsed to 2.45
  QPS. Concurrent host evidence included multiple unrelated `next-server`,
  `npm ci`, and Jest processes using substantial CPU. The controller aborted,
  force-unlocked/completed 323 exact-prefix Graphile jobs, cleaned two tenants
  (5,155 effects and 6,387 deliveries), and direct global verification again
  found zero `soak_%` jobs, locks, actions, effects, or deliveries. It is now
  waiting at the <=25% CPU gate before retrying; no budget was weakened.
- Host load then subsided naturally. The controller observed 24 consecutive
  qualifying CPU samples (final sequence 17.68–23.36%, one active GitLab job
  recorded as evidence only) and started another fresh, non-overridden soak at
  2026-08-04T13:57:19Z. Deployment `staging-56ed662-20260804T1357Z` runs in
  container `engineering-team-soak-24h-mseq2q97`, with both Graphile and
  LangGraph children live and expected completion at 2026-08-05T13:57:19Z.
- The normally admitted soak's window 0 passed. Graphile completed 7,500/7,500
  with enqueue p95/p99 35.623089/118.455293 ms, ready-to-start p95 104 ms, no
  ending pool waiters, and zero residue. LangGraph ran 300.056 seconds at
  concurrency 2, completed 11,995 operations, checkpoint-write p95 was 24 ms,
  and it had zero failures, duplicates, ending pool activity, or cleanup
  residue. Window 1 began with both children live.
- Window 1 of the normally admitted soak passed. Graphile completed 7,500/7,500
  with enqueue p95/p99 43.033093/148.478107 ms, ready-to-start p95 128 ms, no
  ending pool waiters, and zero residue. LangGraph ran 303.220 seconds at
  concurrency 2, completed 12,036 operations, checkpoint-write p95 was 24 ms,
  and it had zero failures, duplicates, ending pool activity, or cleanup
  residue. Window 2 began with both children live.
- Window 2 passed. Graphile completed 7,500/7,500 with enqueue p95/p99
  49.214034/85.030639 ms, ready-to-start p95 179 ms, no ending pool waiters,
  and zero residue. LangGraph ran 300.066 seconds at concurrency 2, completed
  7,834 operations, checkpoint-write p95 was 34 ms, and it had zero failures,
  duplicates, ending pool activity, or cleanup residue. Window 3 began with
  both children live.
- Window 3 passed. Graphile completed 7,500/7,500 with enqueue p95/p99
  43.757402/101.027083 ms, ready-to-start p95 196 ms, no ending pool waiters,
  and zero residue. LangGraph ran 300.318 seconds at concurrency 2, completed
  9,588 operations, checkpoint-write p95 was 30 ms, and it had zero failures,
  duplicates, ending pool activity, or cleanup residue. Window 4 began with
  both children live.
- The normally admitted soak failed at window 4: LangGraph passed (304.335
  seconds, 11,014 completions, checkpoint-write p95 50 ms, zero failures or
  duplicates), but Graphile drained without emitting the required artifact as
  host contention rose again. The controller aborted at 14:26:04Z,
  force-unlocked/completed 30 exact-prefix jobs from interrupted window 5, and
  cleaned 1,425 effects and 1,696 deliveries. Direct global verification found
  zero `soak_%` Graphile jobs/locks, actions, effects, or deliveries; the exact
  execution checkout remained clean at the merge SHA.
- Root-cause review found Graphile's missing report follows its fixed 120-second
  post-load completion timeout; this timeout is not externally configurable in
  the exact merge code. The load container had CPU shares 4096 while the target
  PostgreSQL container still had Docker's default shares amid sixteen other
  long-lived containers. Raised only
  `engineering-team-golden-path-postgres-1` to shares 4096, matching the load
  container, without setting a CPU cap or changing any workload/latency budget.
  PostgreSQL remained healthy and queryable. Patched the external controller to
  verify/reassert matching shares after future controller restarts, and wrote
  revision-bound evidence to
  `.artifacts/staging-rollout-56ed662/container-resource-adjustment.json`.
- While the recovery goal was blocked, the controller later admitted another
  normal soak at 14:49Z; it reached window 5 before another missing Graphile
  artifact at 15:23Z, cleaned successfully, then expired the attempt-9
  checkpoint. A fresh isolated preflight attempt 2 passed at 15:41Z before the
  controller waited for paired-preflight CPU admission.
- At 16:21Z the user explicitly directed `resume ignore the cpu thresholds`.
  Patched the external controller to persistently bypass only host-CPU
  admission for isolated, paired, and soak stages while recording one observed
  CPU sample and active GitLab count at each gate. Workload, latency, duration,
  integrity, and cleanup budgets remain unchanged. Evidence is in
  `.artifacts/staging-rollout-56ed662/cpu-threshold-override.json`; focused soak
  tests passed 5/5 with the controller's actual `NODE_PATH`.
- Restarted the idle LaunchAgent as PID 50096. It reasserted equal 4096 CPU
  shares for load and PostgreSQL, logged the ignored 48.55% CPU / eight-job
  condition, and started fresh exact-SHA isolated Graphile attempt 1 at
  2026-08-04T16:22:15Z (`release_diag_msev948i`).
- Ignored-CPU isolated attempt 1 completed all 7,500 acknowledgements and had
  zero residue, but correctly failed the unchanged ready-to-start p95 budget:
  72,504 ms versus the 2,000 ms limit (enqueue p95/p99
  97.887204/151.087057 ms). No passing artifact was emitted. The controller
  retained the persistent CPU-admission override and scheduled another exact
  retry after 60 seconds.
- Ignored-CPU isolated attempt 2 also completed 7,500/7,500 and cleaned to zero
  residue, but failed unchanged performance budgets: enqueue p95 was
  159.938644 ms (100 ms limit), enqueue p99 242.077367 ms, and ready-to-start
  p95 102,650 ms (2,000 ms limit). The controller logged the failure, ignored
  the next 49.41% CPU / eight-job admission condition, and started exact-SHA
  isolated attempt 3 (`release_diag_msew0ivg`) at 16:43:34Z.
