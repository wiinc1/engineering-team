# 2026-07-27

- The repaired release resume controller became quiescent and automatically
  advanced the pinned `56ed662b…` staging rollout. Isolated Graphile attempt 2
  passed 7,500/7,500 with zero residue. The paired preflight passed Graphile
  7,500/7,500 and LangGraph 13,560 runs with zero failures, duplicates, or
  cleanup residue.
- A fresh 24-hour soak started at `2026-07-27T04:38:58.800Z`, but shared-host
  load returned. After 7h32m, only 4 of 69 Graphile windows had passing
  artifacts while all completed LangGraph windows passed. Because the soak
  counts every failed window, it could no longer produce valid zero-violation
  evidence.
- Stopped only the invalid scoped soak; unrelated Next/GitLab/indexing
  workloads were not interrupted. Removed 3,487 residual scoped soak
  deliveries and 2,749 effects left by interrupted/failed windows, then
  verified zero scoped residual rows.
- Repaired the external controller artifact without changing release commit
  `56ed662b…`: soak processes now run in a dedicated process group, conclusively
  failed windows trigger an early abort, partial artifacts remain preserved,
  and failed soaks loop back through strict quiescence plus isolated and paired
  preflights instead of exiting permanently after 24 hours. The monitor
  captures the exact soak tenant prefix before terminating its dedicated
  process group and transactionally cleans only residual rows from that run.
- Controller syntax passed and its pure window detector correctly identified
  the preserved missing Graphile artifact for window 3. Re-armed LaunchAgent
  `com.engineering-team.runtime-release-resume-56ed662`; the latest re-arm is
  healthy at PID 95132 with empty stderr. The second resumed-goal audit reached
  60 samples with zero consecutive at or below 25%; the latest 12 readings were
  40.13%–44.33%. No new preflight child started, and the previously interrupted
  soak's scoped residual counts remain zero.
- Third consecutive resumed-goal audit confirmed the same external blocker.
  The controller reached 72 samples with zero consecutive readings at or below
  25%; the latest 12 were 39.40%–44.78%. LaunchAgent PID 95132 remained healthy
  with empty stderr, no controller child or new preflight artifact existed, and
  unrelated Next/npm CI workloads were left running. Formally marked the
  release goal blocked again; the durable controller remains armed to resume
  automatically when the host becomes quiescent.
  State is again `waiting_for_quiescence` for `isolated_graphile`.
