# 2026-07-26

- Continued the Graphile/LangGraph release objective from staging commit
  `56ed662bfb3df538d402ff7045f17ebae4e5d867`.
- Diagnosed the durable resume controller failure: `runChild()` passed a
  `fs.createWriteStream()` to `child_process.spawn()` before the stream's file
  descriptor had opened, causing `ERR_INVALID_ARG_VALUE` for `stdio`.
- Repaired
  `/private/tmp/engineering-team-staging-56ed662-uFNvVM/.artifacts/staging-rollout-56ed662/resume-after-quiescence.js`
  to synchronously open the log descriptor, pass it to `spawn`, close the
  parent's descriptor, and append lifecycle results safely.
- `node --check` passed. An actual `runChild()` self-test using `/dev/null`
  exited `0` and wrote both start and exit records to
  `preflight/controller-launch-self-test.log`.
- Re-armed LaunchAgent
  `com.engineering-team.runtime-release-resume-56ed662`; it is running and its
  state is `waiting_for_quiescence` for `isolated_graphile`.
- The strict gate is working: the first 36 five-second samples stayed above the
  25% threshold, most recently approximately 40–80%, with host load averages
  `16.64/15.89/12.60`. Unrelated shared-host activity was not interrupted.
- Next sequence remains unchanged: five consecutive <=25% CPU samples,
  isolated Graphile five-minute preflight, repeat quiescence gate, paired
  Graphile/LangGraph five-minute preflight, repeat quiescence gate, then start a
  new 24-hour soak from zero and monitor it through final evidence generation.
- After three consecutive resumed-goal audits, the same external host condition
  remained. The controller was healthy at PID 21965 and had evaluated 60
  five-second samples, but still had zero consecutive samples at or below 25%;
  recent samples were 30.12–65.74% and host load averages were
  `17.08/16.66/13.42` while unrelated `partner_portal` CI workloads were
  active. Formally marked the resumed release goal blocked. The repaired
  LaunchAgent remains running and will advance automatically when the strict
  host gate passes.
