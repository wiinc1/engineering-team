# 2026-07-07

- Software Factory strict real-delivery proof must not target Vercel. The factory runtime is the coordinated stack: local/operator-machine Docker Postgres, audit API, audit workers, UI, forgeadapter, and real OpenClaw when explicitly pointed at a live gateway.
- Supabase is not part of the factory stack except local/development Postgres context. Treat legacy Vercel/Supabase references as compatibility or stale docs unless current runbooks explicitly require them.
- Misstep to avoid: do not infer deployment target from `vercel.json`, `.vercel`, Vercel API wrappers, or old Supabase wording. Re-derive the proof target from Milestone A/E runbooks and the user's stated stack.
