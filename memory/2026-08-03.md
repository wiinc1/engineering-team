# 2026-08-03

- Status audit found the Graphile/LangGraph release rollout is no longer being
  monitored. LaunchAgent `com.engineering-team.runtime-release-resume-56ed662`
  is loaded but stopped with exit code 1. Its August 2 stderr reports
  `MODULE_NOT_FOUND` because the temporary staging worktree was removed; only
  the empty stdout and stderr logs remain under the rollout artifact directory.
- No completed 24-hour soak or final release evidence was found. The last
  durable result remains the July 27 failed soak and cleanup, so production
  cutover is still unapproved and the rollout is stalled rather than merely
  waiting for host quiescence.
- The primary workspace remains on divergent branch
  `feat/canonical-owner-projection-drain` at `eb54196` (one commit ahead and 199
  commits behind GitLab `origin/main` at `56ed662`). It has four modified
  observability files and 982 untracked files, largely staging artifacts and
  task/memory documents.
- Recreated the exact release SHA in durable worktree
  `/Users/wiinc2/.openclaw/workspace/engineering-team-staging-56ed662`, restored
  dependencies, recovered the final resume controller from session history,
  and repointed the LaunchAgent away from `/private/tmp`. Controller syntax and
  its real child-launch self-test pass; the repository soak unit suite passes
  5/5. Staging Postgres remains healthy at migrations 001–022 with zero
  `soak_%` or `release_%` delivery/effect residue.
- Re-armed LaunchAgent `com.engineering-team.runtime-release-resume-56ed662`.
  It is running at the strict `isolated_graphile` quiescence gate. Its first 12
  five-second CPU samples were 34.32%–76.70%, so it correctly has not started a
  performance preflight while unrelated `partner_portal` GitLab/Next workloads
  are loading the shared host.
- After the user explicitly authorized a busy-host run, consumed a one-shot CPU
  gate override and ran the unchanged isolated Graphile preflight at exact SHA
  `56ed662bfb3df538d402ff7045f17ebae4e5d867`. All 7,500 jobs were acknowledged,
  but the preflight failed its performance budgets: enqueue p95 101.28225 ms,
  enqueue p99 228.377834 ms, and ready-to-start p95 34,939 ms. Pool peak was 6
  of 10 with no pool waiters at the end. Controller cleanup and direct database
  verification found zero Graphile jobs and zero scoped deliveries, effects,
  or operator actions. The override was single-use; subsequent attempts remain
  protected by the normal CPU quiescence gate.
- The original 25-second CPU-only admission window subsequently admitted two
  more runs during brief lulls while GitLab work was still active. Both
  acknowledged all 7,500 jobs and cleaned to zero residue, but failed unchanged
  performance budgets; the latest had enqueue p95 107.596541 ms and
  ready-to-start p95 70,795 ms. Replaced the fragile admission check with a
  two-minute (24 x five-second) <=25% CPU window that also requires zero active
  direct GitLab-runner jobs. No release threshold or workload was weakened.
- Repointed the restored controller's execution root to the clean exact-SHA
  checkout `/Users/wiinc2/.openclaw/workspace/engineering-team/_checkouts/main-56ed662`;
  the controller and evidence remain external in the staging artifact tree.
  The checkout is source-clean at `56ed662b…`, uses the restored dependency tree
  through `NODE_PATH`, and the LaunchAgent is healthy. At the latest gate audit,
  seven to eight unrelated GitLab jobs remained active, so no new preflight was
  admitted.
- Corrected the LaunchAgent from `ProcessType=Background` to `Standard` after
  local `launchd.plist(5)` documentation confirmed that Background imposes CPU
  and I/O limits inherited by child processes. This removes an unintended
  controller-side performance penalty without using unrestricted Interactive
  priority or changing any load/latency budget. The service restarted cleanly;
  the strict two-minute/no-GitLab-job gate remains in force.
- Third consecutive recovery continuation confirmed the same external blocker:
  CPU fell to 12–22%, but two to three direct GitLab-runner jobs remained active
  or were replaced continuously, so the combined admission counter stayed at
  zero through 48 samples. The exact-SHA controller remained healthy, scoped
  Graphile/delivery/effect/action counts stayed zero, and no invalid preflight
  was started. Marked the active recovery goal blocked per the required audit
  rule; the LaunchAgent remains armed to resume automatically when GitLab jobs
  reach zero for the full two-minute qualification window.
- User resumed the blocked goal and explicitly authorized continuing despite
  load. A one-use stage-sequence authorization ran the isolated host test at
  unchanged 25 QPS/7,500 jobs; it acknowledged all jobs but failed enqueue p95
  183.261375 ms and ready-to-start p95 62,108 ms, then cleaned to zero.
- Built exact-SHA Linux image `engineering-team-runtime-preflight:56ed662`
  (`sha256:e3b9166dbf21…`, OCI revision label exact) with host `node_modules`
  excluded. Two manual isolated runs directly sharing staging PostgreSQL's
  network namespace proved materially better isolation: the first passed
  enqueue p95/p99 at 78.786964/124.255628 ms but narrowly missed ready p95 at
  2,390 ms; the second loaded interval failed at 124.853341 ms enqueue p95 and
  94,662 ms ready p95. Both acknowledged 7,500/7,500 and cleaned to zero.
- Integrated the exact-SHA container execution path into the external durable
  controller for isolated, paired, and soak stages, with image/database
  revision-health checks, direct staging-Postgres networking, bounded CPU
  shares, evidence bind mounts, and explicit container stop on early soak
  failure. Its end-to-end isolated run passed enqueue p95/p99 at
  66.297467/135.673377 ms but failed ready p95 at 10,411 ms during a late
  loaded-host burst; cleanup remained zero.
- Hardened failed container-soak cleanup by recording baseline `soak_%` tenant
  IDs and cleaning only exact tenants introduced by the run when a missing
  first-window Graphile artifact prevents prefix recovery. Live helper check
  returned no soak tenants and `tests/unit/runtime-soak.test.js` passed 5/5.
  Re-armed LaunchAgent PID 79777 at the normal isolated quiescence gate; paired
  and soak override authorizations remain preserved for use only after an
  isolated pass.
- To honor the resumed instruction to continue despite load without weakening
  test gates, changed the optimized container controller's admission policy to
  ignore active GitLab job count while still requiring 24 consecutive
  five-second CPU samples at or below 25%. GitLab counts remain recorded as
  evidence. The first five samples qualified with four jobs active, then a
  27.51–33.44% CPU burst reset the counter; the controller remains armed.
