FROM node:22-bookworm-slim

WORKDIR /app

COPY package*.json ./
RUN npm ci --omit=dev

COPY . .

ENV NODE_ENV=production
ENV AUDIT_STORE_BACKEND=postgres

CMD ["sh", "-c", "node scripts/migrate-audit-postgres.js && node scripts/run-audit-workers.js"]