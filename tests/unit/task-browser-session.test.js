const test=require("node:test"),assert=require("node:assert/strict"),{beginOidcSignIn,buildOidcLogoutUrl,buildAuthHeaders,clearBrowserSessionConfig,clearOidcTransaction,completeOidcSignIn,decodeJwtPayload,hasSessionExpired,isAuthenticatedSession,readAuthRuntimeConfig,readOidcTransaction,readSessionClaims,readBrowserSessionConfig,resolveApiBaseUrl,sanitizeNextRoute,splitRouteTarget,writeOidcTransaction,writeBrowserSessionConfig}=require("../../src/app/session");function createStorage(){const e=new Map;return{getItem(t){return e.has(t)?e.get(t):null},setItem(t,a){e.set(t,a)},removeItem(t){e.delete(t)}}}test("browser session config round-trips through storage",()=>{const e=createStorage(),t=writeBrowserSessionConfig({bearerToken:" abc ",apiBaseUrl:"http://api.local///",expiresAt:"2026-04-10T01:00:00.000Z"},e);assert.deepEqual(t,{bearerToken:"abc",apiBaseUrl:"http://api.local",expiresAt:"2026-04-10T01:00:00.000Z"}),assert.deepEqual(readBrowserSessionConfig(e),t)}),test("buildAuthHeaders only emits authorization when a token exists",()=>{assert.deepEqual(buildAuthHeaders({bearerToken:"jwt-token"}),{authorization:"Bearer jwt-token"}),assert.deepEqual(buildAuthHeaders({bearerToken:"   "}),{})}),test("resolveApiBaseUrl prefers stored value over env fallback",()=>{assert.equal(resolveApiBaseUrl({apiBaseUrl:"http://stored.local"},"http://env.local"),"http://stored.local"),assert.equal(resolveApiBaseUrl({},"http://env.local"),"http://env.local")}),test("decodeJwtPayload decodes base64url claims for display",()=>{globalThis.atob=t=>Buffer.from(t,"base64").toString("utf8");const e=Buffer.from(JSON.stringify({sub:"pm-1",tenant_id:"tenant-a",roles:["reader"]})).toString("base64url");assert.deepEqual(decodeJwtPayload(`header.${e}.sig`),{sub:"pm-1",tenant_id:"tenant-a",roles:["reader"]})}),test("readSessionClaims decodes actor, tenant, and role metadata from the stored token",()=>{globalThis.atob=a=>Buffer.from(a,"base64").toString("utf8");const e=Buffer.from(JSON.stringify({sub:"pm-1",tenant_id:"tenant-a",roles:["pm","reader"],exp:Math.floor(Date.parse("2026-04-10T01:00:00.000Z")/1e3)})).toString("base64url"),t=readSessionClaims({bearerToken:`header.${e}.sig`});assert.deepEqual(t,{sub:"pm-1",tenant_id:"tenant-a",roles:["pm","reader"],exp:Math.floor(Date.parse("2026-04-10T01:00:00.000Z")/1e3)})}),test("isAuthenticatedSession requires a non-expired token with tenant and actor claims",()=>{globalThis.atob=a=>Buffer.from(a,"base64").toString("utf8");const e=Buffer.from(JSON.stringify({sub:"pm-1",tenant_id:"tenant-a",roles:["pm","reader"],exp:Math.floor(Date.parse("2026-04-10T01:00:00.000Z")/1e3)})).toString("base64url"),t=Buffer.from(JSON.stringify({sub:"pm-1",tenant_id:"tenant-a",roles:["pm","reader"],exp:Math.floor(Date.parse("2026-04-08T01:00:00.000Z")/1e3)})).toString("base64url");assert.equal(isAuthenticatedSession({bearerToken:`header.${e}.sig`},new Date("2026-04-09T12:00:00.000Z")),!0),assert.equal(isAuthenticatedSession({bearerToken:`header.${t}.sig`},new Date("2026-04-09T12:00:00.000Z")),!1),assert.equal(isAuthenticatedSession({bearerToken:""},new Date("2026-04-09T12:00:00.000Z")),!1)}),test("hasSessionExpired honors explicit expiry timestamps",()=>{assert.equal(hasSessionExpired({expiresAt:"2026-04-09T11:59:59.000Z"},new Date("2026-04-09T12:00:00.000Z")),!0),assert.equal(hasSessionExpired({expiresAt:"2026-04-09T12:00:01.000Z"},new Date("2026-04-09T12:00:00.000Z")),!1)}),test("sanitizeNextRoute preserves in-app query strings and blocks unsafe redirects",()=>{assert.equal(sanitizeNextRoute("/tasks?view=board"),"/tasks?view=board"),assert.equal(sanitizeNextRoute("/overview/pm?bucket=needs-attention#ignored"),"/overview/pm?bucket=needs-attention"),assert.equal(sanitizeNextRoute("https://example.com"),"/tasks"),assert.equal(sanitizeNextRoute("//example.com/tasks"),"/tasks"),assert.equal(sanitizeNextRoute("/sign-in?next=/tasks"),"/tasks")}),test("splitRouteTarget separates pathname and query string for post-sign-in restore",()=>{assert.deepEqual(splitRouteTarget("/tasks?view=board"),{pathname:"/tasks",search:"?view=board"}),assert.deepEqual(splitRouteTarget("/overview/pm"),{pathname:"/overview/pm",search:""})}),test("clearBrowserSessionConfig removes persisted state",()=>{const e=createStorage();writeBrowserSessionConfig({bearerToken:"jwt",apiBaseUrl:"http://api.local"},e),clearBrowserSessionConfig(e),assert.deepEqual(readBrowserSessionConfig(e),{bearerToken:"",apiBaseUrl:"",expiresAt:""})}),test("readAuthRuntimeConfig prefers runtime overrides and enables internal fallback in test mode",()=>{globalThis.__ENGINEERING_TEAM_RUNTIME_CONFIG__={oidcDiscoveryUrl:"https://idp.example/.well-known/openid-configuration",oidcClientId:"browser-client",oidcScope:"openid profile email offline_access"};const e=readAuthRuntimeConfig({MODE:"test"},globalThis.__ENGINEERING_TEAM_RUNTIME_CONFIG__,{origin:"https://app.example"});assert.equal(e.oidcDiscoveryUrl,"https://idp.example/.well-known/openid-configuration"),assert.equal(e.oidcClientId,"browser-client"),assert.equal(e.oidcRedirectUri,"https://app.example/auth/callback"),assert.equal(e.oidcLogoutRedirectUri,"https://app.example/sign-in?reason=signed_out"),assert.equal(e.oidcScope,"openid profile email offline_access"),assert.equal(e.internalAuthBootstrapEnabled,!0),assert.equal(e.isOidcConfigured,!0),delete globalThis.__ENGINEERING_TEAM_RUNTIME_CONFIG__}),test("OIDC transaction storage round-trips through session storage",()=>{const e=createStorage();writeOidcTransaction({state:"state-1",codeVerifier:"verifier-1",nonce:"nonce-1",next:"/tasks?view=board",apiBaseUrl:"http://api.local///"},e),assert.deepEqual(readOidcTransaction(e),{state:"state-1",codeVerifier:"verifier-1",nonce:"nonce-1",next:"/tasks?view=board",apiBaseUrl:"http://api.local"}),clearOidcTransaction(e),assert.equal(readOidcTransaction(e),null)}),test("beginOidcSignIn stores PKCE transaction state and builds the authorize redirect",async()=>{globalThis.btoa=o=>Buffer.from(o,"binary").toString("base64"),globalThis.crypto=require("node:crypto").webcrypto;const e=createStorage(),t=[];await beginOidcSignIn({config:{oidcDiscoveryUrl:"https://idp.example/.well-known/openid-configuration",oidcClientId:"browser-client",oidcRedirectUri:"https://app.example/auth/callback",oidcScope:"openid profile email",isOidcConfigured:!0},next:"/tasks?view=board",apiBaseUrl:"/api",storage:e,redirect:o=>t.push(o),fetchImpl:async()=>({ok:!0,json:async()=>({authorization_endpoint:"https://idp.example/oauth2/authorize",token_endpoint:"https://idp.example/oauth2/token"})})}),assert.equal(t.length,1);const a=new URL(t[0]);assert.equal(a.origin,"https://idp.example"),assert.equal(a.searchParams.get("client_id"),"browser-client"),assert.equal(a.searchParams.get("redirect_uri"),"https://app.example/auth/callback"),assert.equal(a.searchParams.get("response_type"),"code"),assert.equal(a.searchParams.get("scope"),"openid profile email"),assert.equal(a.searchParams.get("code_challenge_method"),"S256");const r=readOidcTransaction(e);assert.equal(r.next,"/tasks?view=board"),assert.equal(r.apiBaseUrl,"/api"),assert.ok(r.state),assert.ok(r.codeVerifier),assert.ok(r.nonce)}),test("completeOidcSignIn exchanges the code for an access token and restores the next route",async()=>{globalThis.atob=r=>Buffer.from(r,"base64").toString("utf8"),globalThis.btoa=r=>Buffer.from(r,"binary").toString("base64"),globalThis.crypto=require("node:crypto").webcrypto;const e=createStorage();writeOidcTransaction({state:"callback-state",codeVerifier:"callback-verifier",nonce:"callback-nonce",next:"/overview/pm?bucket=needs-routing-attention",apiBaseUrl:"/api"},e);const t=Buffer.from(JSON.stringify({sub:"pm-1",tenant_id:"tenant-a",roles:["pm","reader"],exp:Math.floor(Date.now()/1e3)+3600})).toString("base64url"),a=await completeOidcSignIn({config:{oidcDiscoveryUrl:"https://idp.example/.well-known/openid-configuration",oidcClientId:"browser-client",oidcRedirectUri:"https://app.example/auth/callback",isOidcConfigured:!0},search:"?code=oidc-code&state=callback-state",storage:e,fetchImpl:async(r,o)=>{const i=String(r);return i.endsWith("/.well-known/openid-configuration")?{ok:!0,json:async()=>({authorization_endpoint:"https://idp.example/oauth2/authorize",token_endpoint:"https://idp.example/oauth2/token"})}:(assert.equal(i,"https://idp.example/oauth2/token"),assert.equal(o.method,"POST"),assert.match(String(o.body),/code=oidc-code/),assert.match(String(o.body),/code_verifier=callback-verifier/),{ok:!0,json:async()=>({access_token:`header.${t}.signature`,expires_in:3600,token_type:"Bearer"})})}});assert.equal(a.next,"/overview/pm?bucket=needs-routing-attention"),assert.equal(a.sessionConfig.apiBaseUrl,"/api"),assert.equal(a.claims.sub,"pm-1"),assert.equal(readOidcTransaction(e),null)}),test("buildOidcLogoutUrl appends the logout redirect and client id",()=>{const e=buildOidcLogoutUrl({oidcLogoutUrl:"https://idp.example/logout",oidcLogoutRedirectUri:"https://app.example/sign-in?reason=signed_out",oidcClientId:"browser-client"}),t=new URL(e);assert.equal(t.origin,"https://idp.example"),assert.equal(t.searchParams.get("post_logout_redirect_uri"),"https://app.example/sign-in?reason=signed_out"),assert.equal(t.searchParams.get("client_id"),"browser-client")});
